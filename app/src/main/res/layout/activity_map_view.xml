<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true">

    <fragment
        android:id="@+id/map"
        android:name="com.google.android.gms.maps.SupportMapFragment"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <EditText
        android:id="@+id/placeSearchEt"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="24dp"
        android:layout_marginTop="48dp"
        android:layout_marginEnd="24dp"
        android:background="@drawable/bg_token_search"
        android:drawablePadding="6dp"
        android:drawableTint="@color/red001"
        android:gravity="center"
        android:hint="@string/mapview_search_view"
        android:inputType="textPostalAddress"
        android:padding="12dp"
        android:maxLength="14"
        android:textColor="@color/blue005"
        android:textColorHint="@color/gray002"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />


    <ImageView
        android:id="@+id/customBackIv"
        android:padding="12dp"
        app:layout_constraintStart_toStartOf="@id/placeSearchEt"
        app:layout_constraintTop_toTopOf="@+id/placeSearchEt"
        app:layout_constraintBottom_toBottomOf="@+id/placeSearchEt"
        android:src="@drawable/ic_back_arrow"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"/>


    <androidx.coordinatorlayout.widget.CoordinatorLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginTop="48dp">

        <include layout="@layout/bottomsheet_mapview_gyms_list" />
    </androidx.coordinatorlayout.widget.CoordinatorLayout>

</androidx.constraintlayout.widget.ConstraintLayout>