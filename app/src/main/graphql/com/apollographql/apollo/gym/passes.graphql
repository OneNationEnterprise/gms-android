query Passes($params: PassesFilter){
    passes(params: $params){
        list{
            id
            name
            description
            amount
            colorCode
            image
        }
    }
}